@model CarWebSite.Models.HomeViewModel

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="categories-title">Categories</h1>
</div>

<style>
.categories-title {
    font-size: 60px;
    color: #45322a;
    text-align: center;
    margin-top: 20px;
    padding: 10px;
    border-radius: 5px;
    text-shadow: 2px 2px 2px black;
}
</style>

<div class="categories-section scroll-container">
    <button class="scroll-btn categories-left-btn">◀</button>
    <div class="categories-scroll-content scroll-content">
        @foreach (var category in Model.Categories)
        {
            <div class="category-item">
                <div class="category-text-container">
                    <p class="category-text">@category.Description</p>
                </div>
            </div>
        }
    </div>
    <button class="scroll-btn categories-right-btn">▶</button>
</div>

<div class="text-center">
    <h1 class="brands-title">Brands</h1>
</div>

<style>
.brands-title {
    font-size: 60px;
    color: #45322a;
    text-align: center;
    margin-top: 20px;
    padding: 10px;
    border-radius: 5px;
    text-shadow: 2px 2px 2px black;
}
.scroll-container {
    display: flex;
    align-items: center;
    overflow: hidden;
    width: 100%;
}

.scroll-content {
    display: flex;
    overflow-x: auto;
    scroll-behavior: smooth;
    width: 100%;
}

/* Hide the visual scrollbar */
.scroll-content::-webkit-scrollbar {
    display: none;
}

.scroll-content {
    -ms-overflow-style: none; /* Hide scrollbar in IE/Edge */
    scrollbar-width: none; /* Hide scrollbar in Firefox */
}

.category-item {
    position: relative;
    margin: 20px;
    border-radius: 15px;
    min-width: 150px;
    min-height: 70px;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transition: transform 0.3s ease;
}

.category-item:hover {
    transform: scale(1.05);
}

.category-text-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 50%;
    width: 100%;
    border-radius: 15px;
    background-color: #6E4331;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

.category-text {
    font-size: 14px;
    font-weight: bold;
    color: white;
    margin: 0;
    padding: 12px;
}

.photo-item {
    position: relative;
    margin: 20px;
    border-radius: 15px;
    min-width: 200px;
    min-height: 200px;
    text-align: center;
    background-color: white;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

img {
    width: 50%;
    height: 50%;
    border-radius: 10px;
    object-fit: cover;
    align-items: center;
    background-color: white;
}

.photo-text {
    font-size: 12px;
    color: #333;
    text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
    position: absolute;
    bottom: 10px;
    left: 50%;
    transform: translateX(-50%);
    background-color: rgba(255, 255, 255, 0.7);
    padding: 5px;
    border-radius: 5px;
    visibility: hidden;
}

.scroll-btn {
    cursor: pointer;
    border: none;
    background-color: #994c2b;
    color: white;
    font-size: 20px;
    padding: 10px;
    margin: 10px 10px;
    border-radius: 20px;
}

.scroll-btn:hover {
    background-color: #6E4331;
}
</style>

<div class="brands-section scroll-container">
    <button class="scroll-btn brands-left-btn">◀</button>
    <div class="brands-scroll-content scroll-content">
        @foreach (var photo in Model.Photos)
        {
            <div class="photo-item">
                <img src="@photo.Url" alt="@photo.Description" />
                <p class="photo-text">@photo.Description</p>
            </div>
        }
    </div>
    <button class="scroll-btn brands-right-btn">▶</button>
</div>

<div class="text-center">
    <h1 class="news">News</h1>
</div>

<style>
.news {
    font-size: 60px;
    color: #45322a;
    text-align: center;
    margin-top: 20px;
    padding: 10px;
    border-radius: 5px;
    text-shadow: 2px 2px 2px black;
}
</style>

<script>
// Categories section
const categoriesScrollContent = document.querySelector('.categories-scroll-content');
const categoriesLeftBtn = document.querySelector('.categories-left-btn');
const categoriesRightBtn = document.querySelector('.categories-right-btn');

// Scroll left for categories
categoriesLeftBtn.addEventListener('click', () => {
    categoriesScrollContent.scrollBy({
        left: -200, // Adjusted for smaller category boxes
        behavior: 'smooth'
    });
});

// Scroll right for categories
categoriesRightBtn.addEventListener('click', () => {
    categoriesScrollContent.scrollBy({
        left: 200, // Adjusted for smaller category boxes
        behavior: 'smooth'
    });
});

// Brands section
const brandsScrollContent = document.querySelector('.brands-scroll-content');
const brandsLeftBtn = document.querySelector('.brands-left-btn');
const brandsRightBtn = document.querySelector('.brands-right-btn');

// Scroll left for brands
brandsLeftBtn.addEventListener('click', () => {
    brandsScrollContent.scrollBy({
        left: -300,
        behavior: 'smooth'
    });
});

// Scroll right for brands
brandsRightBtn.addEventListener('click', () => {
    brandsScrollContent.scrollBy({
        left: 300,
        behavior: 'smooth'
    });
});
</script>